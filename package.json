{
  "name": "tradersurvivor99",
  "version": "1.0.0",
  "description": "Trading Journal Profesional con Firebase",
  "main": "index.html",
  "scripts": {
    "start": "http-server -p 8080 --cors",
    "dev": "http-server -p 8080 --cors",
    "build": "echo 'Build completed'",
    "deploy": "vercel --prod",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "trading",
    "journal",
    "firebase",
    "finanzas"
  ],
  "author": "TraderSurvivor Team",
  "license": "MIT",
  "dependencies": {
    "firebase": "^12.3.0"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
